<UserControl x:Class="TeamDashboard.BuildView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="64" d:DesignWidth="256">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <DockPanel Background="#FF82B93A">
        <TextBlock x:Name="BuildNameText" Style="{StaticResource ContentHeaderTextStyle}" DockPanel.Dock="Top" Margin="8,4,0,0">Build name</TextBlock>
        <TextBlock x:Name="FinishTimeText" Style="{StaticResource ContentSoftTextStyle}" Margin="8,0,0,0">5 minutes ago</TextBlock>
        <TextBlock Style="{StaticResource ContentTextStyle}" Margin="4,0,0,0">by</TextBlock>
        <TextBlock x:Name="StartedByText" Style="{StaticResource ContentTextStyle}" Margin="4,0,0,0">User</TextBlock>
    </DockPanel>
</UserControl>
